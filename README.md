# Tcp.Server
`Tcp.Server` is management layer on top of Tcp/IP using RESTful API (Asp.Net Core)

The purpose of this project is to enable maximum control for on the data sent and recieved in TCP channel.

The main clients of the `Tcp.Server` are automated systems that have requirement to sample and inject the data passed in the TCP channel
